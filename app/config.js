const host = 'http://dev.homeadverts.com';

const config = {
  URL_LOGOUT: `${host}/logout`,
  URL_SETTINGS: `${host}/profile/settings/edit/`,
  URL_HELP: `${host}/help/z78`,
  URL_USER_ME: `${host}/api/user/me`,
  URL_USER_COLLECTION: '/api/users.json',
};

export default config;
